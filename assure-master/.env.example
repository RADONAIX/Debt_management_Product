# Same-origin API path. The Vite dev server proxies this to the Assure+_MS
# backend on port 8000 (see vite.config.ts).
# Set an absolute backend URL only when the UI and API are deployed separately.
VITE_API_URL=/api
