.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
dist/
*.egg-info/

# Word lock files, created while a .docx is open.
~$*

# Run output. Regenerated by `eval.run`, and large.
# Note: eval/personas/fixtures/ is NOT ignored — the seeded persona set is
# version-controlled on purpose (§8: metrics across a changing set are
# meaningless). Nor are cassettes or human_ratings.json, which are evidence.
eval/transcripts/
